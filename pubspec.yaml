name: stock_check_entry
description: "A new Flutter project."

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.13.1

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  supabase_flutter: ^2.9.1
  flutter_riverpod: ^3.0.0
  image_picker: ^1.1.2
  mobile_scanner: ^7.0.1
  csv: ^6.0.0
  path_provider: ^2.1.5
  share_plus: ^11.0.0
  cross_file: ^0.3.5
  excel: ^4.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
